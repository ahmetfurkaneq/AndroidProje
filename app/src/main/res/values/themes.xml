<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidProje" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowBackground">@color/theme</item>
        <item name="android:colorBackground">@color/theme</item>
    </style>

    <style name="Theme.AndroidProje" parent="Base.Theme.AndroidProje" />
</resources>